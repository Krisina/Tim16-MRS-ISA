<div class="register-form">
	<div class="form">
		<h1 class = "h5">Editovanje Restorana</h1>
		<%= form_for :restaurant, url: restaurant_path(@restaurant), method: :patch do |f| %>
			<%= f.text_field :name, :placeholder => "ime restorana" %>
			<%= f.text_field :distance, :placeholder => "udaljenost" %>
			<%= f.text_field :rating, :placeholder => "rejting" %>
			<%= f.text_field :friends, :placeholder => "prijatelji" %>
			<%= f.text_field :reservation, :placeholder => "rezervacija" %>
			<%= f.submit :"Edit" %>
		<% end %>
		<% if @restaurant.errors.any? %>
			<div class="alert-box error"><span>error: </span>
				<i class="fa fa-times-circle"></i>
				<% for message_error in @restaurant.errors.full_messages %>
					<li><%= image_tag("error.png") %><%= message_error %></li>
				<% end %>
			</div>
		<% end %>
	</div>
</div>